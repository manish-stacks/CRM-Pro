# Apni ASLI MySQL credentials yahan daalo (upar wale sirf example the).
# Format: mysql://<db_user>:<db_password>@<host>:<port>/<db_name>

# Naya Prisma/Next.js DB (jahan migrated data jaayega)
DATABASE_URL="mysql://root:YOUR_PASSWORD@localhost:3306/hbs_new"

# Purana DB — jisme u119813757_hbs_crm.sql import kiya hai
OLD_DATABASE_URL="mysql://root:YOUR_PASSWORD@localhost:3306/hbs_old"
